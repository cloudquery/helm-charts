apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudquery.config" . }}
  labels:
  {{- include "cloudquery.labels" . | nindent 4 }}
data:
  config.hcl: |-
  {{- .Values.cloudquery.config | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cloudquery.env" . }}
  labels:
  {{- include "cloudquery.labels" . | nindent 4 }}
data:
{{- toYaml .Values.cloudquery.env | nindent 2 }}
