apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cloudquery.secret" . }}
  labels:
  {{- include "cloudquery.labels" . | nindent 4 }}
data:
{{- range $k, $v := .Values.cloudquery.secret }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
