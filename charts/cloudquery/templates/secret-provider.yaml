{{- if .Values.serviceAccount.enabled -}}
apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: cloudquery
spec:
  provider: aws
  secretObjects:
  - secretName: cloudquery-sm
    type: Opaque
    data: 
    - objectName: cloudquery-complete
      key: dsn
  parameters:
    objects: |
        - objectName: "cloudquery-complete"
          objectType: "secretsmanager"
{{- end }}